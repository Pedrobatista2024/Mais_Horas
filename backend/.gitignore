# ===== Backend (Python / FastAPI) =====
# O que vale para o repositório inteiro está no .gitignore da raiz.

# Ambiente virtual
.venv/
venv/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Segredos
.env

# Uploads de usuário
#
# Ficam fora do git. O disco do plano free do Render é efêmero, então em
# produção estas imagens somem a cada redeploy — a correção é storage externo
# (Cloudinary/S3/R2) em app/services/user_service.py, registrada como dívida
# técnica em docs/backend-refactor.md.
uploads/
