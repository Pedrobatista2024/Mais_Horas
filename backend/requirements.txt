# ===== Mais Horas - API (FastAPI) =====

# Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Configuração e validação
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Fuso horário
# O Windows não traz o banco de fusos IANA, e a imagem do servidor pode não
# trazer. Sem isto, ZoneInfo("America/Sao_Paulo") falha no boot (RN-53).
tzdata==2024.2

# Banco
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# Segurança
pyjwt==2.10.1
argon2-cffi==23.1.0
bcrypt==4.2.1
cryptography==44.0.0

# Upload de arquivos
python-multipart==0.0.20

# Certificado (PDF + QR Code)
reportlab==4.2.5
qrcode[pil]==8.0

# ===== Desenvolvimento e teste =====
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
